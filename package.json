{
  "name": "cerin-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:server": "pnpm --filter server dev",
    "cms:seed": "pnpm --filter cms seed",
    "build": "pnpm -r build",
    "dev": "pnpm -r dev"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.4.0",
      "onFail": "download"
    }
  }
}